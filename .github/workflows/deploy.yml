name: Deploy Tree of Life System

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      service:
        description: 'Which service to deploy'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - github-sync
          - linear-sync
          - notion-sync
          - api
          - frontend
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      confirm:
        description: 'Type DEPLOY to confirm'
        required: true
        type: string

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate deployment
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.confirm != 'DEPLOY'
        run: exit 1
      - name: Deployment approved
        run: |
          echo "✅ Deployment approved"
          echo "Event: ${{ github.event_name }}"
          echo "Service: ${{ github.event.inputs.service || 'all' }}"
          echo "Environment: ${{ github.event.inputs.environment || 'staging' }}"

  deploy:
    runs-on: ubuntu-latest
    needs: validate
    # Only run actual deployment for manual workflow_dispatch events
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Deploy ${{ github.event.inputs.service || 'all' }} to ${{ github.event.inputs.environment || 'staging' }}
        run: |
          echo "🚀 Deploying service: ${{ github.event.inputs.service || 'all' }}"
          echo "🎯 Target environment: ${{ github.event.inputs.environment || 'staging' }}"
          echo "📦 Using npm deploy script (Railway)"
          echo "⚠️  Note: Service and environment selection not yet implemented"
          npm run deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Deployment complete
        if: success()
        run: |
          echo "🚀 Deployment successful!"
          echo "Service: ${{ github.event.inputs.service || 'all' }}"
          echo "Environment: ${{ github.event.inputs.environment || 'staging' }}"
          echo "Time: $(date)"
